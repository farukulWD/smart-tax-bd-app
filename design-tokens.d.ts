// Types for the CommonJS token tables in `design-tokens.js`, which
// `tailwind.config.js` also requires at build time.

export declare const FONT_SIZES: Record<string, number>;
export declare const SPACING: Record<string, number>;
export declare const varName: (prefix: string, key: string | number) => string;
